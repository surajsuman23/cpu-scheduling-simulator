import {test} from 'node:test';import assert from 'node:assert/strict';import worker from '../server/worker.mjs';
test('health and unknown route',async()=>{assert.equal((await worker.fetch(new Request('https://test/api/health/ready'),{})).status,200);assert.equal((await worker.fetch(new Request('https://test/api/missing'),{})).status,404)});
test('server calculates algorithms and rejects invalid input',async()=>{const call=data=>worker.fetch(new Request('https://test/api/v1/simulations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),{});const r=await call({processes:'id,arrival,burst\nP1,0,5\nP2,1,3\nP3,2,1',quantum:2});assert.equal(r.status,200);const body=await r.json();assert.equal(body.computedBy,'server');assert.equal(body.results.length,3);assert.equal((await call({processes:'invalid',quantum:0})).status,422)});
